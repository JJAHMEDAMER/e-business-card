import React from "react";
import ReactDOM from "react-dom";

// Style sheet
import "./App.css"

// Components
import App from "./App"
const root = document.getElementById("root");

ReactDOM.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
  root
);
